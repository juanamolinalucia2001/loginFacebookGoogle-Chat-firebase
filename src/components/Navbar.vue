<template>
    <nav>
        <v-app-bar
        app
        class="indigo" 
        dark
        >
    
      <v-app-bar-nav-icon @click="drawer = !drawer"></v-app-bar-nav-icon>

      <v-toolbar-title>ChatBox</v-toolbar-title>
      <v-spacer></v-spacer>

      <v-btn
        @click="cerrarSesion"
        target="_blank"
        text
      >
        <span class="mr-2">CERRAR SESIÓN</span>
        <v-icon>fas fa-user-times</v-icon>
      </v-btn>
    </v-app-bar>
     <v-navigation-drawer class="indigo" v-model="drawer" app dark>

        <v-layout column align-center mt-5>
            <v-flex>
               
            <v-list-item-avatar>
              <v-img :src="usuario.foto"></v-img>
            </v-list-item-avatar>
            </v-flex>
            <v-flex>
                <p class="mt-3 white--text">{{usuario.nombre}}</p>
            </v-flex>
        </v-layout>
        <v-list>
         <v-list-item v-for="(item, index) in items" :key="index" :to="item.to">
             <v-list-item-icon>
              <v-icon>{{item.icon}}</v-icon>
            </v-list-item-icon>

          <v-list-item-content>
            <v-list-item-title>{{item.title}}</v-list-item-title>
            
          </v-list-item-content>
         </v-list-item>
         
        </v-list>
       
      </v-navigation-drawer>

    </nav>
</template>
<script>
import {mapActions, mapState} from 'vuex'
export default {
     methods:{
    ...mapActions(['cerrarSesion'])
  },
  data(){
    return{
        drawer:false,
         items: [
          { title: 'Admin', icon:'far fa-user', to:{name:'admin'}  },
          { title: 'Chat', icon: 'far fa-comment-alt', to:{name:'chat'}  }
        ],
    }
  },
  computed:{
    ...mapState(['usuario'])
  }
}
</script>